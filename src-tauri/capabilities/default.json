{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and timer window",
  "windows": ["main", "timer"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
